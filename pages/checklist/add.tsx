import ChecklistAddForm from "../../components/forms/checklist-add.form";
import Template from "../../components/template";

const ChecklistAddPage = () => {
  return (
    <Template pageTitle="Create new Checklist">
      <ChecklistAddForm />
    </Template>
  );
};

export default ChecklistAddPage;
